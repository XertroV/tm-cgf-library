// maximum players per game room; should match server validation
const uint MAX_PLAYERS = 64;

const uint MIN_PLAYERS = 2;

const uint ROOM_TIMEOUT_MS = 1000;

const uint MAX_TEAMS = 16;
const uint MIN_TEAMS = 2;

const string LATEST_SERVER_MINOR = "0.1.";
